@using Microsoft.AspNet.Identity
@using language
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row">
    <div class="col-md-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>@Resources.Account</h2>              
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <form class="form-horizontal form-label-left">
                    <div class="form-group">
                        <div class="col-md-9 col-sm-9 col-xs-12">
                            <div class="">
                                <label>
                                    <input id="checkReport" name="checkReport" type="checkbox" class="js-switch" checked />@Resources.Send_Report
                                </label>
                            </div>
                            <div class="">
                                <label>
                                    <input type="checkbox" class="js-switch" /> @Resources.Send_Alert
                                </label>
                            </div>                      
                        </div>
                    </div>

                    <div class="ln_solid"></div>
                    <div class="form-group">
                        <div class="col-md-9 col-sm-9 col-xs-12 col-md-offset-3">
                            <button type="reset" class="btn btn-primary">@Resources.Change_Password</button>
                            <button type="submit" class="btn btn-success">@Resources.Save</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<script>
    $(document).ready(function () {

        $('#checkReport').on('change', function () {
            if ($(this).is(':checked')) {
                var aaa = 0;
            }
            else {
                var bbb = 0;
            }
        });

        function ChechFunc() {
            $.ajax({
                url: '@Url.Action("ChangeReportMail", "Profile")',
                type: "post",
                cache: false,
                success: function (result) {
                    debugger;
                }
            });
        }
    });
    });
</script>